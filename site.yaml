- hosts: all
  gather_facts: false
  tasks:
  - include_role:
      role: multi_action
    vars:
      action: backup
  - include_role:
      role: multi_action
    vars:
      action: restore
  - debug:
      var: last_backup
